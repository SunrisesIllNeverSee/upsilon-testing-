name: Upsilon Test Suite

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pydantic httpx

      - name: Run full test suite
        run: pytest -q

  postgres-integration:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: upsilon
          POSTGRES_USER: upsilon
          POSTGRES_PASSWORD: upsilon
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U upsilon -d upsilon"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      TEST_DATABASE_URL: postgresql://upsilon:upsilon@localhost:5432/upsilon

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pydantic sqlalchemy psycopg[binary]

      - name: Initialize database schema
        env:
          PGPASSWORD: upsilon
        run: |
          psql \
            -h localhost \
            -U upsilon \
            -d upsilon \
            -f config/sql/schema.sql

      - name: Run PostgreSQL integration test
        run: |
          pytest -q tests/integration/test_persistence_integration.py
